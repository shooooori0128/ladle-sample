# Button

何の変哲もないボタンです。

## 使用例

```tsx
import Button from "./Button";

export const Component = ({ disabled, onClick }) => {
  return (
    <Button onClick={onClick} disabled={disabled}>
      Click me!
    </Button>
  );
};
```
